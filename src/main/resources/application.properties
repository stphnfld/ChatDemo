spring.datasource.url=jdbc:h2:file:C:/Users/sflood/Documents/db-chat-demo/db-chat-demo;DB_CLOSE_ON_EXIT=TRUE;AUTO_RECONNECT=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=shiro
spring.datasource.password=desune

# Logging
logging.level.org.springframework.security=DEBUG

# Specifically for authentication events
logging.level.org.springframework.security.authentication=DEBUG

# sql logging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type.descriptor.sql=trace
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# timeout for session
app.jwtExpirationMs=850000000

# Hibernate properties
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# H2 Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on_param
